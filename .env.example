# Supabase Configuration
# IMPORTANT: Never commit the actual .env file to version control
# Copy this file to .env and replace with your actual credentials

# Your Supabase project URL (found in Project Settings > API)
SUPABASE_URL=your_supabase_project_url

# Your Supabase anon/public key (found in Project Settings > API)
# This is safe to use in client-side code as it only allows RLS-protected operations
SUPABASE_ANON_KEY=your_supabase_anon_key
